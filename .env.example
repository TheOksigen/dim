# Use a reachable PostgreSQL URL locally. In Coolify, add the real value as a
# secret named DATABASE_URL; do not commit that secret to this file.
DATABASE_URL=postgres://postgres:change-me@localhost:5432/postgres?sslmode=disable
